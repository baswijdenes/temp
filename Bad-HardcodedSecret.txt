<#
.SYNOPSIS
    PILLAR 3 -- BAD example: API key hardcoded in the script.

.DESCRIPTION
    THIS IS WHAT NOT TO DO.

    Once this file ends up in OneDrive / a Git repo / your console history,
    the secret is essentially public:
      - OneDrive keeps version history
      - Git keeps the diff forever, even if you "delete" it next commit
      - PowerShell history (PSReadLine) writes to disk by default

    Run side-by-side with Good-KeyVaultSecret.ps1 to see the contrast.
#>

# Imagine a real script: rotating an SAS, calling a third-party API, etc.
$apiKey = 'sk-live-9f2a47b1-DO-NOT-COMMIT-ME-3c8d'

Write-Host "Calling fake API with key: $apiKey"
# Invoke-RestMethod -Uri 'https://api.example.com/...' -Headers @{ 'x-api-key' = $apiKey }

<#
    Demo invocations -- run from this folder to see how exposed the
    secret is the moment it lives in the file.

    # 1. The "secret" is right there in plain text. Anyone with read
    #    access to the file (or its OneDrive sync, or its git history)
    #    has the key.
    Get-Content .\Bad-HardcodedSecret.ps1 | Select-String 'sk-live'

    # 2. PSReadLine writes every command (and string literal you pasted)
    #    to disk by default:
    $rows = Get-Content (Get-PSReadLineOption).HistorySavePath -Tail 5
#>

